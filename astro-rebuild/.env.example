# Heroku sets DATABASE_URL automatically when you add Heroku Postgres.
# For local dev, use a local Postgres URL or leave unset (API returns 503).
# DATABASE_URL=postgres://user:password@localhost:5432/your_db
